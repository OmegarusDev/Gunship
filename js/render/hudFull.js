/**
 * render/hudFull.js — full sortie HUD draw placeholder.
 * Full HUD draw currently lives in js/app.js registerScreen('sortie').draw (~800 lines).
 * This module will export `drawHud(ctx, cam, state, dt)` and be called from app.js.
 * Keeping the file now completes the render/ split and lets tooling see the layout.
 */
export function drawHud() {
  // TODO: move HUD draw from app.js:2800-3600 here
}
