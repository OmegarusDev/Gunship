/**
 * world/streets.js — street-graph generator (STUB for feature/streets).
 * This file will own StreetGraph { nodes, edges } per site.
 * Currently returns null — the flag in js/config.js keeps main on WORLD_GEN=1.
 * See docs/STREETS_SPEC.md §5 for the full pipeline.
 */
export function generateStreetsForSite(site, worldRoads, seed) {
  // TODO: implement spine + spurs / grid / perimeter as per spec
  return null;
}
